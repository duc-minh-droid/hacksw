# FireLine frontend

React + Vite + MapTiler SDK. See the [main README](../README.md) for setup.

```bash
npm install
npm run dev      # http://localhost:8118
npm run build    # static site in dist/
```

Optional settings live in `.env` (copy `.env.example`). With none set the app runs fully in the browser.
